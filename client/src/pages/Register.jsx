import RegistrationForm from "../components/RegistrationForm";

const Register = () => {
  return (
    <div className="flex h-screen w-full flex-col items-center justify-center bg-registration bg-center ">
      <RegistrationForm />
    </div>
  );
};

export default Register;
